<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CatatanCepat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="floatingActionButtonStyle">@style/MyFabStyle</item>
        <item name="toolbarStyle">@style/MyToolbarStyle</item>
        <item name="cardViewStyle">@style/MyCardStyle</item>
        <item name="android:fontFamily">@font/quicksand_regular</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MyFabStyle" parent="Widget.Material3.FloatingActionButton.Primary">
        <item name="backgroundTint">#9D8710</item>
        <item name="tint">@color/white</item>
    </style>

    <style name="MyToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/brownish</item>
        <item name="titleTextAppearance">@style/MyToolbarTitle</item>
    </style>

    <style name="MyToolbarTitle" parent="TextAppearance.AppCompat.Title">
        <item name="android:fontFamily">@font/quicksand_bold</item>
        <item name="android:textSize">26dp</item>
    </style>

    <style name="MyCardStyle" parent="CardView">
        <item name="android:backgroundTint">@color/brownish</item>
        <item name="cardCornerRadius">12dp</item>
    </style>
</resources>